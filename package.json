{
  "name": "storybook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "storybook": "export NODE_OPTIONS=--openssl-legacy-provider && start-storybook -p 6006",
    "build-storybook": "export NODE_OPTIONS=--openssl-legacy-provider && build-storybook",
    "chromatic": "npx chromatic --project-token=e28b9a3d2c6c",
    "storybook-docs": "export NODE_OPTIONS=--openssl-legacy-provider && start-storybook --docs --no-manager-cache",
    "build-storybook-docs": "export NODE_OPTIONS=--openssl-legacy-provider && build-storybook --docs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.15.8",
    "@storybook/addon-actions": "^6.3.12",
    "@storybook/addon-essentials": "^6.3.12",
    "@storybook/addon-links": "^6.3.12",
    "@storybook/web-components": "^6.3.12",
    "babel-loader": "^8.2.3",
    "chromatic": "^6.0.5"
  },
  "dependencies": {
    "lit": "^2.0.2"
  }
}
